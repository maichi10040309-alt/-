# データ設計・仕様メモ

このアプリはフレームワークを使わない vanilla TypeScript + Vite 構成です。
状態は `src/store.ts` の `Store` クラスが一元管理し、`localStorage`
(キー: `care-rental-billing-v1`)に丸ごと JSON で保存します。サーバーは
ありません。

## 画面構成(`src/ui/pages/*.ts`)

旧運用の「ボタンが多くて見にくい」「Excelが見にくい」という課題に対応する
ため、左メニューは5項目のみに絞っています。

- ホーム(`dashboard.ts`): 件数サマリーとよく使う操作への導線のみ
- 月次利用入力(`usage.ts`): 営業担当が毎月使う入力画面。品目・数量(単位数)・
  単価を行単位で入力し、「前月の内容をコピー」で反復入力を省力化。
  「Excelから一括取り込み」(`importExcel.ts`)から既存Excelを直接取り込むことも可能
- 請求書(`invoices.ts`): 基準月時点で締められた(=4か月分そろった)未請求
  サイクルの一覧、途中解約や都度現金の方向けの「早期請求」、旧Excelの
  「月遅れ等」シートに相当する `LateAdjustment` の入力・一覧、および
  発行済み含む全請求書の一覧。請求書ごとに作成→表示→発行→印刷の
  詳細画面(`renderInvoiceDetailPage`)へ遷移
- 利用者マスタ(`clients.ts`): 「利用者一覧」「変更履歴」の2タブ構成。
  請求サイクルは全利用者共通の固定カレンダー(3月/7月/11月始まり)なので
  利用者ごとの設定は不要。「変更履歴」タブは旧Excelの「新規・終了」
  「休止」シートに相当する `ClientEvent` の記録・一覧
- レンタル品目マスタ(`items.ts`): 品目と月額単価のマスタ
- 事業所情報設定(`settings.ts`, モーダル): 請求書に印字する自社情報

## 請求書の印刷レイアウト(`renderInvoiceDetailPage`)

利用者から実際に発行している請求書のExcelテンプレート(「原本」シート)を
提供してもらい、印字内容・レイアウトをそれに合わせている。旧テンプレートは
品目単位ではなく **月単位1行** で「負担割合・非課税分・課税分・月別合計金額・
備考」を表示する形式だったため、印刷される `.invoice-sheet` もそれに合わせて
月ごとの品目明細ではなく月ごとの集計行(`monthRowHtml`)を表示するようにした。
備考欄(`InvoiceMonth.note`)は自動入力せず空欄で作成し、請求書詳細画面で
直接書き込める自由入力欄にしている(`.js-month-note`、`change`イベントで
`store.saveInvoice()`に保存)。品目名の自動表示は行わない。

品目単位の内訳は「品目明細を表示(社内確認用)」という `<details>`(`no-print`
クラス付き)にまとめてあり、画面上では確認できるが印刷はされない。

事業所情報の初期値(`store.ts`)は、テンプレートに記載されていた実際の
社名・住所(株式会社グッドライフ / 和歌山市内原876-1)を初期値として設定
している。振込先(`bankInfo`)はテンプレートに存在しない(口座振替と思われる)
ため、設定されていても印刷はされず画面上の社内メモとしてのみ表示する。

## データ型(`src/types.ts`)

- `Client`: 利用者。`copayRatio`(1割/2割/3割/生保)は介護保険品目の
  自己負担額計算に使う
- `RentalItem`: レンタル品目マスタ。`billingType` が `'insurance'`(介護保険
  品目)か `'private'`(自費品目)かを表す
- `UsageEntry`: 利用者×対象月×品目 の実績1行。保存時に品目名・単価を
  スナップショットするため、後から品目マスタを変更しても過去実績は
  変わらない。介護保険品目は `quantity`=単位数・`unitPrice`=円/単位
  (利用者の負担割合から自動算出)、自費品目は `quantity`=1・`unitPrice`=
  金額そのもの、として保存する(`amount = quantity × unitPrice` は共通)。
  `taxCategory`(`'nontaxable'`非課税 / `'taxable'`課税)は品目マスタでは
  なく実績行ごとに手動で持たせている(理由は次項)
- `Invoice`: 請求書。4か月分の `InvoiceMonth`(各月の明細・小計・非課税小計・
  課税小計)と `adjustments`(月遅れ等調整の明細、後述)を保持し、`status` が
  `draft`(下書き)→`issued`(発行済み)に遷移する。`nonTaxableTotal`/
  `taxableTotal` は月次分+調整分を合わせた税区分別合計。`billingCategory`
  (`'insurance'`/`'private'`/`'combined'`)で保険分/自費分/旧形式合算を区別する
  (詳細は後述「保険分・自費分を分けた請求書発行」)
- `ClientEvent`: 利用者の状態変化履歴(新規/追加/変更/減少/交換/終了/休止/
  再開)。旧Excelの「新規・終了」「休止」シートに相当。請求計算には使わず、
  記録・参照専用(利用者マスタの`active`フラグは引き続き手動で切り替える)
- `LateAdjustment`: 月遅れ等の調整1件。本来の提供月(`originalYearMonth`)
  と、実際に請求書へ計上する月(`billedYearMonth`)を別々に持つ。旧Excelの
  「月遅れ等」シートに相当

## 介護保険の自己負担額の考え方

実際の運用Excel(利用者から提供)を確認したところ、この事業所の介護保険
レンタル品目は「単位数 × 10円/単位」が保険給付の基準額で、そのうち
利用者が負担するのは `単位数 × 負担割合(1割=1円/単位、2割=2円/単位、
3割=3円/単位、生保=0円/単位)` という自己負担額のみであることが分かった
(残りは国保連への保険請求分で、本アプリの対象外)。これを
`copayYenPerUnit()`(`src/types.ts`)として実装し、単位数さえ入力すれば
利用者ごとの負担割合から自己負担額(=顧客に請求する金額)を自動計算する。
自費品目(保険対象外のレンタル)は単位数の概念がなく、金額をそのまま
入力する。

## 非課税/課税の区分について

実際の運用Excelには利用者ごとに「非課税」「課税」の集計欄があり、その値は
数式ではなく毎月手入力されていた。品目コードから機械的に判定できないか
検証したが、同じ品目(例: 歩行器)でも利用者によって非課税/課税どちらにも
なっており、単純な品目マスタ属性では再現できないことを確認した(介護保険の
給付限度額を超えた分は課税になるなど、月ごとの利用状況次第で変わるため)。
そのため `taxCategory` は品目マスタではなく `UsageEntry` の各行に持たせ、
月次利用入力画面で行ごとに手動選択する(既定値は非課税)。Excel取り込み時は
`excelImport.ts` の `applyTaxCategories()` が、Excel側の非課税/課税の
集計値(単位数ベース)に一致する行の組み合わせを部分和問題として逆算し、
どの行が課税だったかを復元している(実データで全クライアント分、厳密に
一致する組み合わせが存在することを確認済み)。

## 4か月ごとの請求サイクル計算(`src/utils/billing.ts`)

請求サイクルは全利用者共通の固定カレンダーで、3月・7月・11月始まりの
4か月ごとに区切られる(例: 7〜10月分→11月請求、11〜2月分→3月請求、
3〜6月分→7月請求)。`cycleStartForMonth()` が任意の年月からその月を含む
サイクルの開始月を求める(2000年3月を基準点とし、そこから4か月おきに
区切っているだけなので利用者ごとの設定は不要)。

`getClientCycles(client, usageEntries, invoices, referenceMonth)` が
利用者の最初の利用実績月を起点にサイクルを列挙し、各サイクルについて
以下を判定する。

- `isDue`: サイクル最終月が `referenceMonth` より前(=請求月に到達した)
  なら請求可能とみなす。例えば7〜10月分は、基準月が11月になって初めて
  請求対象になる(10月時点ではまだ表示しない)
- `insuranceInvoice` / `privateInvoice`: そのサイクル(`cycleStartMonth`一致)で
  既に作成済みの、保険分/自費分それぞれの請求書(下記「保険分・自費分を
  分けた請求書発行」参照)
- `combinedInvoice`: この機能追加より前に作成された、保険・自費が1枚に
  混在した旧形式の請求書があれば

「請求書」画面の基準月入力を変えることで、過去/将来のサイクル状況を確認
できる。請求書作成時は `buildInvoiceMonths(cycle, usageEntries, lateAdjustments, items, category)`
が対象4か月分の `UsageEntry` のうち指定区分(`category`: 保険分/自費分)の
ものだけを集計し、月ごとの明細・小計・非課税小計・課税小計・合計金額を持つ
`Invoice` を生成する。

## 保険分・自費分を分けた請求書発行

実運用では、同じ利用者でも介護保険品目と自費品目を両方レンタルしている
場合、保険分と自費分で別々の請求書を発行する(実際の請求書Excelにも
「○○様 自費11.12月分」のように自費のみの請求書が別途存在する)。

- `Invoice.billingCategory`(`'insurance'` / `'private'` / `'combined'`)で
  請求書がどちらの区分かを表す。`'combined'`はこの機能追加より前に作成
  された、保険・自費が1枚にまとまった旧形式の請求書(`migrateState()`で
  区分未設定の既存請求書に自動付与し、内容はそのまま保持する。新規に
  `'combined'`の請求書が作られることはない)
- `LateAdjustment.billingType`(`'insurance'` / `'private'`)で、月遅れ等の
  調整をどちらの請求書にまとめて計上するかを指定する。旧データの移行時は
  品目マスタから同名品目の区分を推測し、見つからなければ保険分として扱う
- 「請求書」画面の「請求対象(未請求)」「早期請求」は、利用者・サイクルごとに
  保険分/自費分を別行として表示する(実績がある区分のみ表示し、どちらか
  一方しかレンタルしていない利用者には1行だけ出る)。旧形式の合算請求書
  (`combinedInvoice`)が既にあるサイクルは、両区分とも請求済み扱いとして
  一覧から除外する
- 印刷される請求書のレイアウト・文言は保険分/自費分とも完全に同じ
  テンプレートを使う(画面上のみ、印刷されないタイトル脇に「(保険分)」
  「(自費分)」のラベルを表示して見分けやすくしている)。自費分の請求書
  では「負担割合」列は空欄にする(自己負担割合の概念がないため)

## 請求書の一括作成・表示されない利用者の確認

「請求対象(未請求)」の各行にチェックボックスを追加し、複数行を選択して
「選択した請求書をまとめて作成」で一括下書き作成できるようにした
(1件ずつ`createInvoiceForCycle`を呼び、`navigateAfter=false`で個別画面への
遷移を抑止して一覧にとどまる)。

「なぜこの利用者が一覧に出てこないのか」を利用者ごとに確認できるよう、
「請求対象(未請求)」カードの下に折りたたみ式の一覧を追加した
(`clientDueStatusLabel()`)。表示中の`dueRows`にも`pastDueRows`(次項)にも
含まれない利用者全員を対象に、以下の優先順で理由を判定して表示する。

1. `paymentMethod === 'cash'`(都度現金のため対象外)
2. 利用実績が1件もない
3. 直近サイクルが`isDue`でない(進行中、締め月翌月になれば対象になる旨と
   その年月を表示)
4. 直近サイクルに`combinedInvoice`(旧形式)がある → 請求済み
5. 保険分・自費分とも既に請求書がある、またはそもそも実績がない → 請求済み

(上記のいずれにも当てはまらず、実は未請求扱いになるはずのケースは
`dueRows`または`pastDueRows`側に含まれているはずなので、この一覧には出てこない)

## 「今回分」と「過去の未請求分」の分離

このソフト導入前から続けてレンタルしている利用者は、旧Excel/紙で
既に請求済みのはずの古い月の実績が(移行時にまとめて入力・取り込みされて)
残っていることがある。その場合、締め済みなのに未請求のサイクルが複数
積み上がり、直近の請求対象と見分けがつかなくなってしまう。

これを避けるため、`renderInvoicesPage()`内で利用者ごとに
`getClientCycles()`が返す**一番新しいサイクルの締め月**
(`latestCycleEndByClient`、`isDue`かどうかは問わない)を求め、未請求行の
`cycleEndMonth`がそれと一致するものだけを「今回分」(`dueRows`)、それより
古いものを「過去の未請求分」(`pastDueRows`)として別カード(初期状態で
開いた`<details>`)に分けて表示する。それぞれ独立してチェックボックス選択
・一括作成ができる(`wireDueSection()`で両セクション共通のイベント配線を
関数化)。

判定は利用者単位(直近サイクルの締め月)であって、行単位で「一番新しい
行」を選ぶのではない点に注意。直近サイクルがまだ`isDue`に達していない
(=`allDueRows`に一切現れない)場合でも、それより古い締め済み未請求サイクルは
すべて「過去の未請求分」に回る。

## 利用者マスタの「最新請求書」列

利用者マスタの一覧に、その利用者の請求書のうち対象期間
(`cycleEndMonth`)が一番新しいもの(同じサイクルで保険分・自費分の
2枚があれば両方)へのリンクを表示する列を追加した
(`latestInvoicesForClient()` / `latestInvoiceCellHtml()`)。作成日時では
なく対象期間の新しさで選ぶのは、過去の未請求分をあとから遡って作成
した場合に「作成日時が一番新しい請求書」と「対象期間が一番新しい請求書」
がずれるため。

## 月遅れ等の調整(`LateAdjustment`)

過去の提供月(`originalYearMonth`)の実績が後日判明した場合、その月の
`UsageEntry` を直接書き換えるのではなく、`LateAdjustment` として別途登録し、
「計上する請求月」(`billedYearMonth`)を指定する。`buildInvoiceMonths()` は
`cycleStartForMonth(billedYearMonth)` がそのサイクルの開始月と一致する
`LateAdjustment` を `adjustments` としてまとめて取り込み、月次の集計とは
別枠(請求書の「月遅れ等の調整」セクション)で表示する。これにより、
既に発行済みの過去サイクルの請求書を書き換えることなく、後から判明した分を
現在進行中のサイクルの請求書にまとめて反映できる(旧Excelの「月遅れ等」
シートと同じ考え方)。

## 都度現金・早期請求(途中解約対応)

実運用では、4か月ごとの請求以外に「都度現金で入金」「途中解約で2〜3か月分
だけ請求」というケースがある。これに対応するため:

- `Client.paymentMethod`(`'cycle'` 通常/`'cash'` 都度現金)を追加。`'cash'`の
  利用者は「請求書」画面の「請求対象(未請求)」に自動では表示されない
  (`renderInvoicesPage`のdueRows集計でスキップ)
- 「請求書」画面に「早期請求」セクションを追加。利用者を選ぶと、その利用者の
  未請求サイクル(締め日に関わらず、今日時点までに実績があるもの)を一覧表示し、
  `isDue`を無視して任意のタイミングで請求書を作成できる。途中解約で4か月
  そろわない場合や、都度現金の方の請求書が必要な場合に使う
- `getClientCycles()`の`horizon`計算に使う基準月には、今日時点の年月
  (`currentYearMonth()`)を渡す。ここに将来の年月を渡すと、実績のない
  空サイクルまで大量に列挙されてしまうため注意(この用途で使うのは常に
  「今日時点で存在するサイクルだけ見せたい」場合)
- 印刷される請求書テーブル(`monthRowHtml`)は、実績のない月(`lines.length===0`)
  の行を出さないようにしている。これにより、4か月そろわない早期請求でも
  実際にレンタルのあった月だけが印字される(旧テンプレートの実例でも
  行数が1〜4行と可変だったことに合わせている)

## 初期データ(`src/data/defaultItems.ts`)

実際の運用Excelで使われていた品目コード(手すり・車いす・特殊寝台・歩行器
など)を初期の品目マスタとして登録済み。介護保険品目は自動計算のため
単価設定は不要。自費品目は目安金額を「レンタル品目マスタ」画面から編集できる。

## Excel一括取り込み(`src/utils/excelImport.ts`, `src/ui/pages/importExcel.ts`)

毎月の運用で使っているExcel(「保険(在宅)」「自費(在宅)」シートを含む
.xlsx/.xlsm)を、ブラウザ上のファイル選択でそのまま取り込める。
`XLSX.read()`(SheetJS, `xlsx` パッケージ)でシートを配列として読み込み、
以下の列位置(実ファイルの構造に合わせて固定)から利用者・品目明細を抽出する。

- 「保険(在宅)」シート: 4列目=利用者名, 5列目=フリガナ, 7列目=負担割合,
  8列目=要介護度, 10列目=居宅介護支援事業所, 11列目=ケアマネ, 15列目以降が
  (単位数, コード)の繰り返しペア
- 「自費(在宅)」シート: 3列目=利用者名, 4列目=フリガナ, 6列目=要介護度,
  7列目=居宅, 8列目=ケアマネ, 12列目以降が(金額, コード)の繰り返しペア
  (自費品目は単位数ではなく金額がそのまま入る)

同一利用者は氏名+フリガナ(空白除去)でマッチングし、既存の利用者マスタが
あれば主要項目を上書き、なければ新規作成する。品目マスタに存在しない
品目コードは自動追加される。取り込みはすべてブラウザ内で完結し、ファイル
の内容がサーバー等に送信されることはない。

シート構成が変わった場合は `excelImport.ts` の列インデックスを調整する
必要がある。

## 実装しなかった旧Excelシート

利用者へのヒアリングの結果、以下は優先度が低いと判断し未実装。

- 「デモ」シート(商品お試し訪問の記録): 請求に関係しない営業活動ログのため
- 「請求額(在宅)」「中尾会計提出用」シート: いずれも他シートからの自動集計
  (数式のみで手入力なし)で、本アプリでは「月次利用入力」の一覧タブや
  請求書画面が同等の内容を提供している
- 「リスト」シート(品目コード・居宅事業所名などのマスタ): 品目マスタ・
  利用者マスタの自由入力欄で代替

## 今後の拡張ポイント

- 請求書のCSV/PDFエクスポート(現状は画面印刷のみ)
- 品目マスタ変更時に過去の下書き請求書へ反映するかどうかの選択
- 複数事業所・複数営業担当でのログイン管理(現状は単一ブラウザ内で完結)
- 超過単位数(月間上限超過分)や国保連請求向けの計算は対象外
  (既存のカイポケ等の保険請求システムとの併用を想定)
